{
  "name": "@fortemi/example-graph-svg-vanilla",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "EX-01 — render a hand-authored CommunityGraph to interactive SVG with zero framework and no database.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fortemi/graph": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^7.3.1"
  }
}
