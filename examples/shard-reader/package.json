{
  "name": "@fortemi/example-shard-reader",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "EX-08 — the Knowledge Shard portability loop: bake a shard from an in-browser DB, then browse it read-only with useShard (no PGlite on the reader side).",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fortemi/core": "workspace:*",
    "@fortemi/examples-shared": "workspace:*",
    "@fortemi/react": "workspace:*",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.5.0",
    "typescript": "^5.8.0",
    "vite": "^7.3.1"
  }
}
