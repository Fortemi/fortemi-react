{
  "name": "@fortemi/example-custom-canvas-renderer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "EX-15 — render a CommunityGraph to a hand-written <canvas> with bakeRenderGraph. Proves the data-prep layer feeds any surface, no built-in view, no DB.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fortemi/examples-shared": "workspace:*",
    "@fortemi/graph": "workspace:*",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.5.0",
    "typescript": "^5.8.0",
    "vite": "^7.3.1"
  }
}
