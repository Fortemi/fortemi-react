# @bytecask/* packages are published to public npmjs (canonical) and mirrored
# on the internal Gitea registry. Resolve from npmjs so installs work outside
# the internal network (e.g. the GitHub npm-publish workflow).
