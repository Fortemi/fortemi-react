# fortemi-react v2026.9.6

## Packages

- `@fortemi/core@2026.9.6`
- `@fortemi/graph@2026.9.6`
- `@fortemi/react@2026.9.6`

All packages remain in version lockstep. Graph and React carry the coordinated
version; these search and evidence changes are in Core.

## Typed Search And Citations

Search adapters preserve typed metadata predicates and source scope, enforce
declared capabilities, and apply supported prefilters before ranking. Search
citations bind to validated source evidence; malformed remote evidence fails
at the boundary. The candidate authority receipt pins merged producer schema,
vector and OpenAPI bytes at an immutable revision. Historical captured receipts
and capability-promotion flags are unchanged.

This also includes native JSON schema loading and release-tooling lint fixes.

## Native Test Efficiency

Independent native restore tests clone a pristine empty migrated PGlite schema.
Each clone checks empty native, lineage, archival and queue state. All145cases,
legacy migration fixtures, existing deadlines and global coverage gates remain.

Six additional database-heavy suites use independent clones of immutable empty
migrated schema bytes. Each clone verifies empty native, lineage, archival and
queue state and exact migration identity. Native fixture tests verify vector
availability, data/schema isolation and independent database ownership.

Test scheduling starts the two measured long-running native suites early even
with a cold cache. Other files retain Vitest's ordering within each project, and
the inherited shard membership is unchanged. No cases, assertions, coverage
thresholds, worker bounds or deadlines are removed or relaxed.

## Release And Test Reliability

The offline local browser wrapper uses a cached immutable Playwright image and
validates the actual container's two-CPU, 8-GiB, zero-swap and 256-task bounds.
An internal deadline bounds execution independently of the Docker client. All
browser projects remain enabled; reports and resource-event receipts are retained
before removing only the owned container. Shared hosts still require a separately
approved detached controller and admission checks.

Core CI retains bounded progress diagnostics alongside its native test and coverage
artifacts. Partition completeness and coverage thresholds are unchanged. The release
checklist now names the configured `pnpm test:workspace` gate, including Graph,
React and example unit suites; partial execution or a timeout does not pass it.

## Verification Boundaries

The base source c07b0433 passed all11 PR CI60067 checks before merge, including
coverage aggregation, browser checks and build. Its source-qualified installed
candidate passed220groups against the unreleased producer e99e4723, plus worker,
SSE and browser acceptance. Those receipts retain their original source and
package hashes; they are not executions of this versioned release candidate.

Final release-source CI, clean-installed artifact checks, separate OpenBao
release-tag authority and actual internal/public registry and release/mirror
byte verification remain required. No tag or publication is asserted here.

Published-producer search behavior, the common declared-adapter/mode matrix,
hosted authorization and native-platform qualification remain separate gates.
No broader issue closure or AI-quality qualification follows from this release.
Fortemi remains the Knowledge Shard authority. `core-v1`, `full-v1` and
`record-v1` retain their existing evidence boundaries; the suite audit remains
**NO-GO** for unqualified parity, complete backup or portability.

Never replace published2026.9.5 artifacts to deliver these changes.
